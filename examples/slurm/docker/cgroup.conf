# cgroup v2 for the job steps: memory constrained per step, so the
# allocation's memory is a hard ceiling fiberd's ladder must stay under.
CgroupPlugin=cgroup/v2
ConstrainCores=no
ConstrainRAMSpace=yes
ConstrainSwapSpace=yes
AllowedSwapSpace=0
ConstrainDevices=no
IgnoreSystemd=yes
