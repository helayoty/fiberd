// Package gvisortest holds the integration tests of the gvisor backend on
// the host runtime: real runsc sandboxes, cgroups and checkpoints. They
// run inside the Linux dev container (make linux-test) and skip elsewhere.
package gvisortest
